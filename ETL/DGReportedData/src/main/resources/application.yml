server:
  port: 5000

spring:
  main:
    allow-bean-definition-overriding: true
  datasource:
    url: jdbc:mysql://localhost:8080/data?useLegacyDatetimeCode=false&useUnicode=true&characterEncoding=utf8&characterSetResults=utf8&useSSL=false&allowPublicKeyRetrieval=true
    username: hxzh
    password: 1Q2w3e4r5t!@
    initialization-mode: never
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect  ## org.hibernate.dialect.MySQL5InnoDBDialect
      format_sql: false
    show_sql: false
  data:
    mongodb:
      uri: mongodb://localhost:8080/data

dongguan:
  account:
    accessId: test
    accessPwd: test
  report:
    url: report.data.com